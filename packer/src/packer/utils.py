"""Deprecated test helpers.

Moved to `packer/tests/helpers.py`. Import from there in tests.
"""

# Note: kept as a placeholder to avoid import errors for external tooling that
# may reference `packer.utils`. Prefer `from packer.tests.helpers import ...` in tests.
