[project]
name = "manga-creator"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = [
    "packer",
    "convertor",
]

[dependency-groups]
dev = [
    "pytest-cov>=7.0.0",
]
